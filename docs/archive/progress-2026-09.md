# Progress Log — 2026-09 아카이브

[현재 진행 기록](../PROGRESS_LOG.md)의 이전 항목을 보존합니다.

## 2026-09-03 — 실증 스크린샷을 대시보드 관측으로 교체 (F5)

- **Status**: 사용자가 *"실증 스크린샷은 grafana나 prometheus 등 대시보드에서 관측된 값"* 이라고 지적. 앞서 만든 `proof-w5-01~06`은 터미널 로그 카드였습니다. Prometheus 대시보드 캡처 3장(`proof-w5-07~09`)을 추가하고 글에 **5-B장**을 신설했습니다. 게이트 green(문서 3종 + labs 120건).
- ★ **되짚어 찍기는 실패했습니다.** 이 클러스터의 스크랩 주기는 **15초**인데 F1~F2의 각 측정은 10~20초짜리 버스트입니다. F1d에서 **0.998**까지 갔던 KV 사용률이 Prometheus 기록에는 **0.078**까지밖에 없습니다 — 피크가 스크랩 사이로 빠졌습니다. `docs/STATUS.md` Open Risks가 지적하던 함정을 그대로 밟았습니다.
- **그래서 같은 세 상태를 한 구간에 3분씩 다시 걸었습니다** (`run_f5.sh`). 숫자를 새로 만드는 게 아니라 이미 낸 결론이 모니터링 화면에서 어떻게 보이는지를 남기는 것입니다.

  | 구간 | 구성 | 생성 tok/s | 실행 | 대기 | KV 사용률 | 누적 선점 | GPU% |
  |---|---|---|---|---|---|---|---|
  | A | BF16 util 0.85 · decode | 1,703.6 | 16 | 0 | 0.031 | 0 | 99 |
  | B | FP8 util 0.85 · decode | **2,269.3** | 16 | 0 | 0.028 | 0 | 99 |
  | C | BF16 util 0.33 · prefill | 224.0 | **4** | **60** | **0.812** | **19** | 99 |

- **대시보드가 본 A→B는 +33.2%**로 클라이언트의 +33.7%와 맞습니다. 그리고 이 화면에는 *"배칭이 촘촘해져서"* 라고 말할 여지가 없습니다 — **실행 중인 요청 수가 양쪽 다 16으로 눈에 보이게 같고** KV 사용률은 오히려 FP8이 약간 낮습니다.
- ★ **GPU 사용률은 세 구간 모두 99%입니다.** 처리량이 224에서 2,269까지 열 배 차이 나는 동안 사용률은 같았습니다. 글 7-1에 *"사용률은 GPU가 노는지만 말하고 무엇을 하느라 바쁜지는 말하지 않는다"* 를 추가했습니다.
- **캡처 경로에서 세 가지가 걸렸습니다** (전부 `prom_shot.py`·`_pf_dashboards.sh` 주석에 박아 뒀습니다).
  - **NodePort 30001/30002는 Windows에서 안 잡힙니다** — kube-proxy가 iptables 규칙만 두고 실제 listening 소켓을 만들지 않아 WSL2 localhost 포워딩이 중계할 대상을 못 찾습니다. `kubectl port-forward`(127.0.0.1 바인딩)면 잡힙니다. `--address 0.0.0.0`은 불필요합니다.
  - **9090은 Windows 예약 포트 범위(9021~9120)** 라 바인딩이 막힙니다(`netsh interface ipv4 show excludedportrange protocol=tcp`). 9009로 옮겼습니다.
  - **Prometheus 3.x는 `/query?g0.tab=graph&g0.res_type=auto`** 라야 쿼리가 실행됩니다. `/graph?g0.tab=0`은 페이지는 뜨지만 **빈 패널**이 나옵니다.
- **Grafana는 쓰지 않았습니다** — 로그인이 필요하고 비밀번호를 대신 입력하지 않습니다. 익명 접근도 꺼져 있습니다(`grafana.ini`에 설정 없음). 필요하면 사용자가 직접 로그인해 DCGM 대시보드를 캡처하면 됩니다.
- ⚠️ **조립기가 이전 편집을 두 번 덮어썼습니다.** `_workspace/.../assemble.py`는 `sections/`를 우선하도록 만들어 두었는데, 글에 직접 넣은 편집(5장 커널 이미지·7-1 선점 지침·부록 4건)이 재조립에서 날아갔습니다. `sections/` 쪽에 다시 적용해 복구했고 전수 대조로 확인했습니다. **이후 편집은 글에 직접** 합니다.
- **Changed**: `articles/처리량이 올랐다면 무엇이 빨라진 것인가.md`(589줄, 5-B장 신설) · `articles/screenshots/proof-w5-07~09.png` · `labs/wsl2-vllm-baseline/run_f5.sh`·`_pf_dashboards.sh` · `results/f5-*` · `results/f-analysis.md`(F5 절) · `_workspace/2026-09-02-001/prom_shot.py`.
- **Changed (문서 정리, 체크포인트)**: `docs/NEXT_PLAN.md` **148→131줄** — Priority 0에서 완료분(⓪ 4건·F1a~F1d·F2·F5·그림·글)을 걷어내고 **열린 것만**(⑥ 발행 · ⑦ 공유 · Grafana 선택 · F3 이월) 남겼습니다. 낡은 일정·계획 시제도 결과 시제로 교체. `docs/AGENT_BRIEF.md`의 `▶ NEXT SESSION:`을 *"⓪ 선행 미착수"* 에서 *"발행·공유만 남음"* 으로 갱신(57→59줄). `docs/DECISIONS.md`에 2026-09-03 항목(되짚기 대신 재측정 · Grafana 미사용).
- **Verified**: 게이트 green. 배포는 기준선(BF16 · util 0.85 · **59.50x**)으로 복구. 대시보드 수치와 클라이언트 수치 대조(+33.2% vs +33.7%).
- **Blockers**: 4주차 ⑦ 링크 공유 미확인. 5주차 ⑥ 노션 발행·⑦ 공유 남음. 포트포워딩(9009·3000)이 백그라운드로 떠 있습니다 — `pkill -f _pf_dashboards.sh`로 정리합니다.
- **Next**: 노션 발행 → 링크 공유.

## 2026-09-02 — 5주차 실험 설계를 **확인에서 반증으로** 개정 + `study/` 가드레일 복구

- **Status**: 게이트 green — 문서 3종 + labs **106건**. 인덱스 재생성 **1207 nodes / 98 docs**(`--only md`, Inference Engineering의 `llm-claude-code-korean` 보존 확인). 측정 없음, 커밋 없음.
- ★ **`study/` 가드레일이 깨져 있던 것을 발견하고 복구했습니다.** ① 5주차 원문 `Ch9.md`(2,392줄)·`Ch10.md`(956줄)가 **`knowledge/`에 미추적으로** 놓여 있었습니다 — `knowledge/`는 커밋 대상이고 `MD_SKIP_DIRS`에도 없어서, 그대로 뒀으면 **다음 커밋에 원문이 들어가고 인덱스에 발췌가 박혔을 자리**입니다. `study/`로 옮겼습니다. ② `.gitignore`에서 **`study/` 패턴 줄이 사라져 있었고**(주석만 남음) 커밋 `ae10b8e "study"`에서 **`study/Ch1~Ch8.md` + `LLM기초.md` 9개가 실제로 커밋**됐습니다. 패턴은 복구했지만 **이미 추적 중인 9개는 아직 그대로**입니다 — `git rm --cached`가 필요합니다.
- **5주차 F1을 관찰에서 제거 실험(ablation)으로 바꿨습니다.** 사용자가 *"실험 시나리오를 더 흥미롭게 할 수 없냐"* 고 물어 재설계 — 초판은 교재 주장(*"GEMM 커널 시간은 거의 동일"*)을 **확인**하는 구조라 결론이 시작 전에 이미 보였습니다. 4주차가 값어치를 가진 건 **가설이 뒤집혀서**(수용률 100%인데 −53.7%)였습니다.
  - **F1a** KV 예산 곡선 — `redeploy GPU_MEMORY_UTILIZATION=...` 4~5점 스윕으로 좌표계를 만든다.
  - **F1b ★ 예산 동결 제거 실험** — 양자화 팔의 `Maximum concurrency`를 BF16의 ±5%로 깎고 **이득이 사라지는지** 본다. 관찰이 아니라 *"이득을 껐다 켰다 할 수 있다"*.
  - **F1c** — `prefill` 고동시성에서 **부호가 뒤집히는지** 사냥(W4A16은 compute-bound에서 역양자화가 순비용).
- ★ **개정의 실질 이득은 안전판입니다.** 초판 최대 리스크가 *"컨테이너 안 Nsight 권한이 안 잡히면 축이 흔들린다"* 였는데, **F1b는 Nsight 없이 결론을 냅니다.** F2가 통째로 날아가도 글이 섭니다.
- **F3는 흡수, F4는 잘라냈습니다.** F3(`replica=2`)는 예산이 반이므로 **F1a 곡선 위의 검증점**이 됩니다. F4(Multi-LoRA) 절단 비용은 **0** — `study/Ch10.md:957`은 **Multi-Model(멀티모달) 서빙**이지 Multi-LoRA가 아니고, Multi-LoRA는 도전과제 목록에 없습니다.
- **도전과제 대조를 처음으로 전수로 했습니다** (`Ch10.md:948~957`, 8종). F2가 950·956을, F3가 954·955를 덮고, 952는 3분의 1(`Router→Replica=2`)만. **951(MoE, GPU 4장)은 불가**, **953(Semantic Router)·957(Multi-Model)은 설계에 없음** — F4를 자른 자리의 대체 후보는 953입니다.
- **그림 하나로 합칩니다** — 가로 KV 예산 · 세로 처리량. BF16 곡선 위에 양자화 기본 / 예산동결 / `replica=2` / ★ **4주차 draft-0.5B(−42.2%)·ngram(−3.4%)** 를 얹습니다(4주차 분은 기존 `results/`에서, **추가 측정 0회**). 4주차 결론(토큰 예산)과 5주차 결론(KV 예산)이 한 화면에서 이어집니다.
- **Changed**
  - `docs/plans/2026-08-30-week5-attribution.md` — **§5~§9 전면 교체**(147→183줄) + 개정 배너 + §0을 "사흘 지남"으로 갱신.
  - `docs/NEXT_PLAN.md` — Priority 0 측정 체크리스트를 F1a·F1b·F1c 구조로 교체, 일정 재조정(09-02 ⓪+F1a / 09-03 F1b+F1c / 09-04 F2 / 09-05 F3·초고), 잘라내기 순서 갱신, 「한계」 절에 **FP4/Blackwell 문단** 추가 지시.
  - `docs/DECISIONS.md` — 2026-09-02 항목 신규(근거 5·대가 3·되돌리는 법).
  - `docs/AGENT_BRIEF.md`·`docs/STATUS.md` — 개정 반영, 지연 하루→이틀, `study/` 가드레일 사고 기록.
  - `.gitignore:25` — `study/` 복구. `knowledge/Ch{9,10}.md` → `study/`.
  - `index/knowledge_structure.json` 재생성.
- **Verified**: `make check` green(문서 3종 + labs 106건). `git check-ignore`로 `study/Ch9.md`·`Ch10.md`가 `.gitignore:25`에 잡히는 것 확인. `make index-md` 후 `summary_method` 4종 전수 확인(보강본 보존). 계획서의 인용 줄번호를 옮긴 파일에 대조 — `Ch10.md:500`·`:950`·`Ch9.md:84`는 정확, **`Ch9.md:10`은 한 줄 밀려 `:9`로 정정**. **GPU 측정은 하지 않았습니다.**
- **Blockers**: ⚠️ **4주차 ⑦ 링크 공유 여전히 미확인**(마감 08-30 09:00에서 사흘 경과). 5주차 ⓪ 선행 미착수 — **이틀 지연**, 마감까지 3일 12시간. `study/Ch1~Ch8` 9개가 아직 추적 중(`git rm --cached` 대기). AWS 쿼터 `0` 유지.
- **Next**: 4주차 제출 확인 → 09-02 밤 ⓪ 선행(keeper → Nsight 권한 1h 상한 → 양자화 팔) + F1a 곡선 스윕.

## 2026-09-02 (2) — 5주차 측정 전량 실행 + 과제 글 완성

- **Status**: F1a·F1a2·F1b·F1c·F2 **전부 완료**, 글 `articles/처리량이 올랐다면 무엇이 빨라진 것인가.md`(약 520줄) + 그림 1종 + 인증샷 5장. 게이트 green(문서 3종 + labs **85건**, `test_summarize_trace.py` 14건 신규). 설계의 하루치 계획(⓪+F1a)을 한 세션에 F2까지 밀어넣었습니다.
- ⓪ **FP8이 첫 시도에 떴습니다.** `--quantization fp8`은 BF16 체크포인트를 기동 시 W8A8로 바꾸므로 **추가 다운로드 0**이고 모델·이미지·실행 경로가 그대로라 통제가 오히려 세졌습니다. GPTQ·AWQ는 시도하지 않았습니다. KV 예산 6.51→7.68 GiB, `Maximum concurrency` 59.50x→70.20x(**+18.0%**).
- ★ **F1a — 곡선이 아니라 평평한 선이 나왔습니다.** `GPU_MEMORY_UTILIZATION`만 움직여 KV 예산을 **3.80배**(64,048→243,696토큰) 돌렸는데 `decode` c=16 처리량 변동 **0.5%**, `prefill` c=64 프롬프트 처리량 변동 **5.3%(추세 없음)**. 요청당 KV 수요가 예산의 4%도 안 되기 때문이고, 프리필은 예산을 넘겨도 **연산 바운드**라 초당 토큰이 안 늘어납니다.
  - 1차(decode 전용)에서 평평한 것을 보고 **수요를 예산 가까이 올린 워크로드**(prefill c=64 + `--unique-prefix`)를 같은 배포에서 이어 재도록 스크립트를 고쳤습니다(롤아웃 10회→5회).
  - `util 0.90`은 **기동 자체가 안 됩니다** — 자유 메모리 10.79 GiB인데 요구도 10.79 GiB(윈도우 데스크톱이 1.2 GiB 점유). 상단 천장은 0.85, 대신 0.45를 밑에 붙였습니다.
- ★★ **F1b — 제거 실험이 결론을 냈습니다.** FP8의 KV 예산을 BF16과 같은 자리로 조이고(선형 보정 3회, 70.20x→55.99x→**60.95x**, 오차 2.4%로 ±5% 통과) 다시 쟀습니다.
  - ① BF16 59.50x **1,689.3** tok/s · ② FP8 70.20x **2,258.8**(+33.7%) · ③ **FP8 예산동결 60.95x 2,261.0(+33.8%)**
  - **③이 ①로 주저앉지 않았습니다 — 예산 기여분 0%.** 판정 기준을 실험 전에 정해 둬서 해석의 여지가 없습니다. ①의 3회 재현 편차 **0.38%**(σ 3.3).
  - 이득의 자리는 **ITL** — 0.0097→0.0073초(**−24.7%**). 스텝 수가 아니라 **스텝 하나**가 빨라졌습니다.
- **F1c — 부호는 안 뒤집혔고 크기가 규칙적으로 줄었습니다.** 4주차 E1과 같은 격자 8칸 전부 이득(**+11.1% ~ +35.4%**). decode 34.6→35.4→34.3→**15.1%**, prefill 33.5→29.8→24.6→**11.1%**. 가중치 읽기를 배치가 나눠 쓰므로 배치가 커질수록 아낄 몫이 줍니다. 4주차 추측 디코딩(+194.8% ~ −52.8%)과 대조됩니다.
- **F2 — 도구를 두 번 갈아탔고, 결론은 커널에서 확인됐습니다.**
  - `nsys`·`ncu`가 **이미지에 아예 없습니다**(권한이 아니라 부재). 설계의 중단 기준대로 PyTorch Profiler 단독으로 축소.
  - `VLLM_TORCH_PROFILER_DIR`은 v0.23.0에서 **없어졌습니다** — `/start_profile`이 404. 설정이 `--profiler-config.*` CLI로 옮겨갔고(`vllm/config/profiler.py`), 거기 있는 `delay/max_iterations`로 트레이스를 1 MB 아래로 묶었습니다(`with_stack`은 기본 켜짐이라 껐습니다).
  - ★ **총합 비교는 부호까지 거꾸로 읽힙니다** — 503.1 vs 622.8 ms라 FP8이 24% "더" 쓴 것처럼 보이지만, 두 트레이스가 담은 구간이 58스텝과 98스텝입니다. 스텝당으로 나누면 **GEMM 279.54→202.21 us(−27.7%)**, **어텐션·KV 캐시 14.79→14.78 us(−0.1%)**, 합계 **−26.7%**.
  - **합계 −26.7%가 밖에서 잰 ITL −24.7%와 맞습니다.** 줄어든 시간은 전부 선형 계층에 있고 KV 경로는 미동도 없습니다 — F1b 결과가 커널 단위로 그대로 보입니다.
  - **교재와 다른 결과입니다.** CH9은 양자화 전후 GEMM 시간이 거의 같았다고 적었는데 여기선 27.7% 빨라졌습니다. 모순이 아니라 **커널이 바뀐 것**입니다(`cutlass_80_wmma_tensorop_bf16` → `enable_sm89_to_sm90<cutlass::gemm>`, Ada FP8 텐서코어). W4A16처럼 저장만 줄이는 방식이면 GEMM은 그대로일 수 있습니다.
- **4주차 판단 하나를 정정했습니다.** 지난주 draft-0.5B 팔을 *"KV 예산이 42% 줄어 손해의 원인을 분리할 수 없다"* 며 본문에서 뺐는데, 그 팔의 **34.38x는 이번 스윕 구간(15.64x~59.50x) 안**이고 그 구간의 처리량 변동은 0.5%였습니다. 예산 기여가 0이므로 −49.7%는 전부 추측 디코딩 값이었습니다 — **재지 않고 배제했던 것**입니다. 글 6장에 그대로 적었습니다.
- **Changed**
  - `labs/wsl2-vllm-baseline/` — 측정 스크립트 6종(`run_f1a.sh`·`run_f1a2.sh`·`run_f1b.sh`·`run_f1c.sh`·`run_f1d.sh`·`run_f2.sh`) + 체인 `run_fall.sh` + 런처 `_launch.sh`, 분석 도구 `summarize_trace.py`(+테스트 14건).
  - `results/` — `f1a*`·`f1b*`·`f1c*`·`f2*` 원본 + 분석 `f-analysis.md`.
  - `tools/make_figures.py` — `fig_scatter_curve` 추가(가로축이 연속량인 산점도) → `articles/figures/fig-f1-kv-budget-curve.svg`.
  - `articles/처리량이 올랐다면 무엇이 빨라진 것인가.md` + `articles/screenshots/proof-w5-01~05.png`.
  - `study/` 9개 **`git rm --cached`** — 09-02 (1) 항목이 남긴 잔여 작업을 닫았습니다.
- **Verified**: 게이트 green(`check_docs.py` 3종 + pytest). 팔마다 기동 로그 `Maximum concurrency` 기록 후 비교 성립 판정. 예산 동결은 ±5% 기준(4주차와 같은 기준)으로 판정. BF16 재현 3회 0.38%. F2는 정규화 후에만 비교.
- **Blockers**: ⚠️ **4주차 ⑦ 링크 공유 여전히 미확인**(사용자만 가능). 5주차 ⑥ 노션 발행·⑦ 공유 남음. F3(복제 2개)는 미착수 — GPU 1장에 파드 둘을 올리려면 device plugin 우회가 필요해 시간 대비 우선순위를 낮췄습니다. AWS 쿼터 `0` 유지.
- **Next**: F1d(예산 하한까지 내려 꺾이는 점 찾기) → 노션 발행 → 링크 공유.

## 2026-09-01 — 맥 vs GPU 비교를 5주차에서 빼고 **설계만 확보** + 게이트 구멍 하나 메움

- **Status**: 게이트 green — 문서 3종 + labs **106건**. 인덱스 재생성 **1200 nodes / 98 docs**(`--only md`, Inference Engineering의 `llm-claude-code-korean` 보존 확인). 측정 없음, 커밋 없음.
- ★ **게이트가 `test_manifest.py` 15건을 실행하지 않고 있었습니다.** `Makefile:25`에 파일명이 빠져 실제 실행은 **91건**인데 `docs/STATUS.md`는 106건으로 적고 있었고, *"`test_manifest.py`가 `EXTRA_ARGS` 전개를 감시합니다"* 도 **사실이 아니었습니다.** 5주차 F1이 `EXTRA_ARGS`를 집중적으로 쓰기 직전에 발견. 한 줄 수정으로 71+6+19+10 = **106건 실행 확인.** `meta.summary_method`에 이어 **문서의 주장과 게이트가 어긋난 두 번째 사례**입니다.
- **맥(M4 Max) vs GPU 비교를 검토하고 5주차 과제에서 뺐습니다.** 사용자가 "과제로 다루면 어떤가"를 물어 판단 — ① 통제 변수가 무너지는데 5주차 축이 하필 *원인 귀속*이라 정반대 ② CH9·CH10 범위 밖 ③ 마감 5일에 F1·F2를 깎는 교환. 폐기가 아니라 **보류**로 두고 설계만 확보.
- **비용 전제가 확인됐습니다** — `benchmark.py`가 **OpenAI 호환 + `--base-url`**(`:560`, `/v1/models` 자동 탐색 `:129`)이라 맥 엔드포인트에 **코드 수정 0**으로 붙습니다. 하네스를 새로 만들 필요가 없다는 게 이 계획이 성립하는 이유이고, 깨지면 중단 조건(⓪-2)입니다.
- **하드웨어 실측**: M4 Max · 48GB 통합 · GPU 40코어 · CPU 16(12P+4E) · `iogpu.wired_limit_mb=0`(기본) · 디스크 296Gi 여유. **llama.cpp·MLX 미설치, ollama는 설치돼 있음**(`/usr/local/bin/ollama`, 배칭 통제가 불투명해 스모크용으로만).
- **설계의 핵심은 교란 통제**입니다. 한 번에 변수 5개(하드웨어·런타임·커널·양자화 포맷·스케줄러)가 바뀌므로 **절대 수치의 플랫폼 간 비교는 결론 근거에서 배제**하고, 견디는 셋만 씁니다 — 능력 판정(이분법) · **곡선의 모양**(배칭 구현의 성질) · 플랫폼 **내부 비율**(TTFT:ITL, 절대 성능이 약분됨).
- **이월분과 연결**: llama.cpp `--parallel N`이 컨텍스트를 N등분한 고정 슬롯으로 잡는다면 그게 **배칭 4종 표의 `static` 칸**입니다. 가설로 적고 G2에서 확인.
- **Changed**
  - `docs/plans/2026-09-01-unified-memory-vs-vram.md` 신규(129줄) — G 시리즈 4종 · 교란 통제 · 스택 선택(`llama-server` 우선) · ⓪ 선행 5 · 실행 트리거 · 중단 기준 · **하지 않을 것 4건**.
  - `Makefile:25` — `test_manifest.py`를 `check-labs`에 추가.
  - `docs/NEXT_PLAN.md` — Priority 2에 보류 항목(착수 금지 + 트리거 A/B), Priority 0 ⑤에 **5주차 「한계」 절 한 문단**(Nsight·vLLM 커널이 CUDA 전용이라 F2는 애플 실리콘에서 원리상 재현 불가, **추가 측정 0회**).
  - `index/knowledge_structure.json` 재생성.
- **Verified**: `make check` green — 문서 3종(links·index·tools) + labs **106건**(수정 전 91건 확인 → 수정 후 106건 확인). `make index-md` 후 `summary_method` 4종 전수 확인. **GPU 측정은 하지 않았습니다.**
- **Blockers**: ⚠️ **4주차 ⑦ 링크 공유 여부 여전히 미확인**(마감 08-30 09:00 경과). 5주차 ⓪ 선행 미착수 — 계획상 08-31 밤이었으므로 **하루 지연**, 마감까지 5일. AWS 쿼터 `0` 유지.
- **Next**: 4주차 제출 확인 → 5주차 ⓪ 선행(keeper 기동 → Nsight 권한 1시간 상한 → 양자화 팔).
