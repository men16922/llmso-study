# AGENTS.MD

이 저장소는 CloudNet@ LLMSO 스터디의 한국어 학습 문서와 재현 가능한 실습 자산을 관리합니다. 애플리케이션 저장소가 아니므로, 문서의 기술적 정확성·재현성·출처 구분을 우선합니다.

## 작업 원칙

- 새 문서와 설명은 자연스럽고 명확한 한국어로 작성합니다. 기술 용어인 LLM, GPU, API, KV Cache, TTFT 등은 억지로 번역하지 않습니다.
- 실험 수치, 날짜, 버전, 모델명, 명령어는 근거 없이 바꾸지 않습니다. 해석과 관측 사실을 구분하고, 추정에는 추정이라고 표시합니다.
- 스터디 과제용 글은 `배경과 질문 → 핵심 용어 → 가설과 실험 설계 → 결과 → 운영 해석 → 한계 → 재현 절차 → 참고 자료` 흐름을 기본으로 삼습니다. 글의 성격에 맞지 않으면 필요한 항목만 사용합니다.
- 표는 설정이나 수치를 비교할 때, 목록은 실제로 나열이 필요할 때만 사용합니다. 핵심 결론은 글 앞부분의 짧은 요약과 마지막 결론에서 확인할 수 있게 합니다.
- 링크와 이미지에는 내용을 알 수 있는 설명을 붙이고, 저장소 안의 자료는 상대 경로로 연결합니다.

## 검증 명령

마크다운을 추가하거나 수정한 뒤 저장소 루트에서 다음 명령을 실행합니다.

```bash
python3 tools/build_pageindex.py --only md
```

PDF까지 포함한 전체 인덱스 재생성은 LLM으로 보강한 한국어 요약을 발췌문으로 되돌릴 수 있으므로, 문서 작업에는 `--only md`만 사용합니다.

검색과 구조 확인에는 다음 명령을 사용합니다.

```bash
python3 tools/search_index.py "KV cache"
python3 tools/search_index.py --outline inference --depth 3
```

## 민감 자료와 외부 전송

- 저장소는 비공개 유지가 원칙입니다. `knowledge/references/pdf/`와 `index/*.json`에는 외부 공개에 주의해야 할 자료가 포함되어 있습니다.
- 원본 노션이나 제3자 자료를 그대로 전재하지 않습니다. 학습한 내용을 자기 언어로 정리하고, 필요한 출처를 표시합니다.
- 공개 원격 저장소로 푸시하거나 외부 서비스에 문서를 업로드하기 전에는 반드시 사용자에게 확인합니다.

## 참고 문서

- 저장소 개요와 과제 목적: `README.md`
- 도구와 인덱스 구조, 깨지기 쉬운 지점: `CLAUDE.md`
- 스터디 규칙: `knowledge/03-study-rules.md`
- 인덱스 사용법: `index/USAGE.md`
