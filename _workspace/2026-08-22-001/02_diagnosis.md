# 진단 — 2026-08-22-001

## 장르·레지스터
- 장르: 블로그 (기술 실측 기록 — 개인 스터디 과제)
- 격식: 합쇼체(합니다체) — 문서 전체가 일관되며 혼재 없음. **윤문은 이 격식을 유지한다(양방향 불변)**
- 성격 주의: 논증 자체는 사람이 쓴 것에 가깝다(가설 → 반증 → 미확인 명시). risk_band=low(score 3)와 일치한다. **AI 티는 논리가 아니라 표면 서식·수사 반사에 몰려 있다.** 아래 4개만 겨냥하고, 그 밖은 손대지 않는다.

## 지배 패턴 (겨냥 순서)

1. **J-1** 과도한 볼드 — 이 글에서 가장 강한 단일 AI 티. 본문 문단 대부분이 볼드를 최소 1개, 흔히 2~3개 품는다. 게다가 볼드가 붙는 자리가 거의 항상 "문장의 결론 반쪽"으로 고정돼 있어 강조가 아니라 리듬 기계가 된다. · 근거: "**그런데 이 표는 틀렸습니다.**", "**A와 B는 거의 붙어 갑니다.**", "**셋 다 절반만 맞았습니다.**", "**동시성 16까지는 100%, 32에서 무너집니다.**" — 문장 자체가 이미 단정형이라 볼드가 정보를 더하지 않는다.
   → 처방: 문단당 볼드 1개 이하로 줄인다. 종결이 이미 단정("~입니다"·"~틀렸습니다")인 문장의 볼드를 우선 해제한다. **표 안·⚠️ 경고 블록·> 인용 판정문의 볼드는 기능적이므로 유지.**

2. **C-8** 대칭 대구 "A가 아니라 B" 반복 — 임계(3회+)를 다섯 배 넘긴다. 글 전체의 논증이 사실상 이 한 틀로만 표현되어, 발견마다 같은 문형이 되돌아온다. J-1과 얽혀 "A가 아니라 **B**입니다" 형태로 굳어 있는 것이 특히 티가 난다. · 근거: 본문 약 17회 — "계층의 가격이 아니라 **계층 + 엔진 버전 차이의 합**입니다", "노브가 아니라 **도착률의 함수**입니다", "어텐션 헤드 수가 아니라 **KV 헤드 수**입니다", "버전 탓이 아니라 계층 탓입니다", "공짜가 아니라 **동시 수용 인원을 파는 것**입니다", "결과가 아니라 **비교를 성립시키는 일**", "정적 공식이 아니라 런타임 측정 결과", "\"설정에서 켜면 되는 기능\"이 아니라 **모델 export 시점에 결정되는 성질**"
   → 처방: 절반 이상을 다른 형태로 흩는다 — ① B만 단언하고 A는 앞 문장에서 이미 부정된 것으로 처리(2문장 분할) ② "A로 보이지만 실제로는 B" ③ A 부정 자체를 삭제하고 B를 그대로 서술. 결론(§정리)의 대구 3연속은 반드시 깬다.

3. **C-11** 연결어미 뒤 쉼표 — 정량 앵커에서 유일하게 강한 이상치(z=+3.81, ★ S1 트리거). 실제로 "-고,"·"-지만,"·"-니,"·"-면," 뒤 쉼표가 반복 확인된다. · 근거: ending_comma_rate 0.34 (z=+3.81) / "이미지가 같으니 엔진도 같고, 남는 변수가 계층 하나로 좁혀집니다", "슬롯을 늘리면 배치가 커지고, 배치가 커지면 중간 텐서가 커지고, 그만큼 ~", "그러니 상한에 닿을 일이 없고, 상한을 낮춰도 아프지 않습니다", "유용하지만 입도가 다르고, 엔진 내부는 안 보입니다", "첫 번째만 체감했지만, 레플리카가 여럿인 환경에서는 ~", "동시성 1이면 같이 묶일 요청이 애초에 없으니, 설정한 시간만큼 ~"
   → 처방: 연결어미 직후 쉼표를 원칙 삭제. 절이 길어 호흡이 필요하면 쉼표를 살리는 대신 문장을 둘로 끊는다. 대등 항목 나열("요청이 길수록, 동시 요청이 많을수록")의 쉼표는 대상이 아니다.

4. **J-3** 대시(—) 부가 설명 반복 — 본문에서 "문장입니다 — 부연" 패턴이 15회 안팎. 하나하나는 살아 있는 구어체 장치지만, 이 밀도에서는 문장 확장 수단이 대시 하나로 획일화된 인상을 준다. · 근거: "모델은 LLM이 아니라 `mobilenet_v2`입니다 — 그게 요점입니다", "다만 정직하게 적어두면 — 이번에 관측한 변동은 ~", "20 ms 설정에 p50 26.8 ms — 대기 20 ms + 연산 약 7 ms입니다", "~ 쌓이는 궤적 — **이 환경에서는 볼 수 없습니다.**", "교재가 각주로 예고해 둔 그대로입니다 — ~"
   → 처방: 본문 대시의 3분의 1~2분의 1만 괄호·쉼표·문장 분리로 바꾼다. **전량 제거 금지** — 이 글의 대시 일부는 진짜 구어 리듬이다. 헤딩("7-4. 복잡도 — 그리고 관측성")·표 캡션·참고 자료 목록의 대시는 손대지 않는다.

## 정량 앵커 (코드가 센 것 — 확정 증거)
- ending_comma_rate: 0.34 (z=+3.81, ★ S1 → **C-11**) — 위 3번의 근거. 이 글의 유일한 강한 이상치.
- have_make_literal_count (A-7): 2 — "모델이 배치 축을 갖고 있어야 하고" 등. **문맥상 자연스러운 소유 서술로 오탐에 가깝다. 우선순위 최하, 억지 치환 금지.**
- double_particle_count (A-19): 2 — "교재에서의 자리"(표 헤더, 보존 대상), "동시성 1에서의 p50 지연". 본문 쪽 1건만 선택적으로 손질 가능.
- relative_clause_nesting (A-18): 3 — 3건뿐이고 문장이 이미 읽힌다. 지배 패턴 아님, 관찰 기록만.
- pronoun_density (A-16): 0.001 / double_passive (A-8): 0 / by_passive (A-9): 0 / deul_overuse (A-17): 0 — **번역투 계열은 사실상 깨끗하다. A 카테고리를 겨냥하지 말 것.**
- lexical_diversity 0.50 (z=-2.93) — **오탐.** 처리량·계층·동시성·상한·KV 같은 기술 용어를 일관되게 반복하는 것이 이 장르의 정확성 요건이다. 다양화 시도는 의미 훼손이므로 **명시적 비대상**.
- conclusion_pivot_count 0 / safe_balance_count 0 / hanja_nominalizer z=-1.58 — D-1·G-3·F-4 계열 무해. 겨냥 금지.

## 보존 지침 (이 글에서 건드리면 안 되는 것)
- **수치 전량(831개)**: 표 안 값, tok/s·TTFT·GiB·%·버전 번호(0.23.0 / 0.7.2 / 2.44.1 / v1.36.2+k3s1), 32.6%·64.02x·±17%·6배 등 서술 속 수치 포함. 반올림·표기 통일 시도 금지.
- **헤딩 48개 문구 불변**: 목차 앵커 링크(`#3-결과--계층의-가격` 등)가 헤딩 텍스트에 묶여 있다. 헤딩은 한 글자도 바꾸지 않는다. 목차 리스트도 동일.
- **마크다운 표 전체 / 코드블록(```bash·```python·```protobuf·plain) / `> ` 인용 블록 / `<details>` 부록의 셸 명령**: 윤문 대상 제외. 표 안 볼드도 제외.
- **⚠️ · ★ · ✅ 기호**: C-5(이모지 남발)로 오판하지 말 것. 경고 블록·주의 지점 표식으로 기능하는 관례적 용법이다. 유지.
- **인라인 코드 식별자**(`max_num_seqs`, `Maximum concurrency`, `gpu_memory_utilization`, `num_key_value_heads` 등) 및 링크 URL: 원형 유지.
- **격식체(합니다체)** 및 1인칭 실측 기록의 관찰자 어조("~했습니다", "미확인으로 남습니다") 유지. 단정을 완곡(G-1)으로 바꾸지 말 것 — 저자의 확신·유보 구분이 이 글의 핵심 가치다.
- **과윤문 경계**: 본 글은 low risk다. 위 4개 패턴 외 카테고리(A·D·E·G·H·I·F)는 **손대지 않는다.** 변경률이 5%를 크게 넘으면 과윤문을 의심할 것.
