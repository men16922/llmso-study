# 진단 — 2026-08-15-001

## 장르·레지스터
- 장르: **리포트** (실측 데이터 기반 기술 실습 보고서 / 스터디 과제 글)
- 격식: **한다체** (`~다` 평서체) — **윤문은 이 격식을 유지한다(양방향 불변)**
- 특이사항: 이 글은 **이미 1차 윤문을 거친 상태**다. 따라서 남아 있는 AI 티는 "생성 원문의 티"가 아니라 **윤문 과정에서 덧씌워진 티**일 가능성이 높다. 처방은 "더 다듬기"가 아니라 **덜어내기** 방향이어야 한다.

## 지배 패턴 (겨냥 순서)

1. **J-1** 과도한 볼드 — 이 글을 가장 강하게 지배한다. 표 셀 강조를 빼고 **본문 산문만 봐도 볼드가 없는 문단이 손에 꼽는다**. 더 심각한 것은 볼드의 용법이 "핵심 단어 강조"를 넘어 **문장 전체를 통째로 볼드 처리하는 결산 마커**로 굳어졌다는 점이다. 소절마다 같은 자리에서 같은 방식으로 반복되어 리듬을 기계화한다.
   · 근거: 5-2 마지막 `**처리량 곡선이 꺾이는 지점과 TTFT가 폭발하는 지점은 같은 사건의 두 얼굴이다.**` / 5-3 `**이 표에서 1주차에 남긴 의문이 풀린다.**` / 5-6 `**결과는 예상과 달랐지만, 오히려 여기서 중요한 단서를 얻었다.**` — 5-1~5-9 아홉 소절 전부가 이 형식의 볼드 결산 문장을 최소 1개씩 갖는다.
   → 처방: **표 안의 수치 볼드와 헤딩 볼드는 전량 보존**하고, 산문 볼드만 문단당 최대 1개로 줄인다. **문장 전체 볼드는 해제**한다(볼드를 벗기면 문장 자체가 약해지는 곳은, 볼드가 아니라 문장이 문제였던 자리다). 6·7절의 볼드 밀도가 특히 높으니 우선 겨냥.

2. **C-8** 대칭 대구 공식 "A가 아니라 B" 반복 — 수사 층위의 최대 지배 패턴. 문서 전체에서 **20회 이상** 같은 틀이 돌아온다. 개별 문장은 정확하고 힘이 있지만, 이 정도 밀도면 독자는 내용이 아니라 **틀의 반복**을 먼저 알아챈다. AI 경구 생성기의 가장 대표적인 지문이다.
   · 근거: `"일하고 있음"이지 "꽉 찼음"이 아니라는 뜻`(1절) / `GPU를 더 빠르게 만드는 게 아니라, 이미 치른 메모리 읽기 비용을 여러 요청이 나눠 내게 하는 것`(2절) / `연산 병목이 아니라 메모리 대역폭 병목`(2절) / `단순 성공률이 아니라 SLO를 지킨 요청의 비율`(4절) / `지켜야 할 지표는 ITL이 아니라 TTFT다`(5-4) / `큐가 아니라 고정 오버헤드다`(5-6) / `p50이 아니라 평균 ITL로`(5-6) / `큐는 E2E의 잔차가 아니라 TTFT 그 자체로`(5-8) / `성능 튜닝 노브가 아니라 용량 정의다`(6①) / `점진적 열화가 아니라 명확한 변곡점`(6①) / `비용은 ITL이 아니라 TTFT로 청구된다`(6③) / `실패가 아니라 쓰레기 숫자`(7절) / `배칭 때문이 아니라 이벤트 루프 때문`(8절)
   → 처방: **전부 없애지 않는다** — 이 글의 핵심 주장 두세 개(2절 "메모리 읽기 비용 분담", 6①"용량 정의")에서만 살리고, 나머지는 평서 단정으로 푼다. 예: `큐가 아니라 고정 오버헤드다` → `이 잔차의 정체는 고정 오버헤드다`. 한 소절 안에 두 번 나오는 곳(5-6)은 반드시 하나로 줄인다.

3. **C-4 + C-6** 문단 첫 문장 요약 공식 / 헤딩 아래 안내문 — 거의 모든 소절이 `헤딩 → 표·그림 → 안내 한 문장 → 실제 해석` 순서로 짜여 있고, 그 "안내 한 문장"이 **아래 내용을 미리 요약할 뿐 정보를 더하지 않는다.** 영어 작문의 topic sentence 관습이 그대로 남은 자리이며, **1차 윤문에서 삽입된 완충 문장일 공산이 크다**(아래 과윤문 소견 참조).
   · 근거: `먼저 요청 하나가 서버에 들어와 나갈 때까지 어떤 과정을 거치는지 살펴보자.`(2절) / `아래에서 수치로 확인해 보자.`(1절) / `세 열의 양상은 서로 다르다.`(5-1) / `이 결과는 5-1의 표와 함께 봐야 한다.`(5-2) / `여기서는 앞의 세 표와 다른 양상이 나타난다.`(5-4) / `이 표에서 눈여겨볼 점은 두 가지다.`(5-5) / `지금까지 살펴본 것은 short 시나리오다.`(5-7) / `앞에서 다룬 값은 모두 클라이언트 측 측정치다.`(5-8) / `이 두 줄에 핵심 결과가 드러난다.`(5-8) / `실험의 한계도 함께 밝혀 둔다.`(한계)
   → 처방: **정보 없는 안내문은 삭제**한다. 표·그림 바로 뒤에는 해석이 오는 게 자연스럽다. 다만 앞 절과의 **연결 정보를 실제로 담은 문장**(5-8의 "5-6과 이어서 읽어야 할 대목이다")은 보존한다 — 그건 안내문이 아니라 논증 연결이다.

4. **J-3** 대시(—) 남용 — 산문 문장 안의 부연 대시가 **18회 내외**로, 소절마다 최소 1회씩 돌아온다. 여기에 헤딩 부제 대시(`## 1. 문제 정의 — 1주차에 남긴 숙제` 형식, 14개 헤딩 전수)까지 겹쳐 문서 전체가 하나의 구두점 습관에 잠겨 있다. 영어 em-dash appositive의 직역 리듬이다.
   · 근거: `고정 오버헤드다 — HTTP 왕복, kubectl port-forward 한 단계, 토큰화`(5-6) / `ITL은 여기서도 둔감하다 — slots=64 c=32에서…`(5-7) / `둘 중 하나만으로는 부족하다 — 캡처만 있으면…`(5-9) / `그 천장을 눈으로 보여준다 — 16에서 딱 멈추고…`(6①) / `B2에서는 더 선명하다 — 부하를 4배 올려…`(6②) / `이 문장의 그림이다 — running 16명은…`(6③) / `가장 이른 신호다 — 5-8에서 이 값이…`(7절) / `공통점이 있다 — 직전에 다른 파드가…`(한계3)
   → 처방: **헤딩의 대시는 절대 건드리지 않는다**(TOC 앵커 연동 — 보존 지침 참조). 산문 대시만 절반 이하로 줄이되, 대체 방법을 섞는다: 콜론, 쉼표+접속, 또는 문장 분리. 특히 **볼드 문장 끝에 대시를 물려 부연하는 조합**(J-1+J-3 중첩)이 6·7절에 몰려 있으니 그 자리부터.

5. **D-3** 열거 도입 공식 "N가지다 / N가지로 나눌 수 있다" — 새 단락을 여는 방식이 거의 자동화돼 있다. 7회 이상 반복되며, 그때마다 곧바로 불릿·번호 목록이 뒤따라 문서 리듬을 예측 가능하게 만든다.
   · 근거: `가설은 세 가지였다.`(1절) / `방식은 네 가지다.`(2절) / `세 가지 장치를 넣었다.`(4절) / `이 표에서 눈여겨볼 점은 두 가지다.`(5-5) / `결과는 네 가지로 정리할 수 있다.`(6절) / `재현 과정에서 마주치기 쉬운 함정 세 가지`(부록)
   → 처방: 절반은 **개수를 말하지 않고 바로 목록으로 들어간다**(개수는 목록을 보면 알 수 있다). 나머지는 개수 대신 **내용**을 앞세운다. 예: `방식은 네 가지다.` → `배치를 언제 닫느냐로 갈린다.` 단, 1절의 `가설은 세 가지였다`처럼 **실험 설계상 개수 자체가 정보인 자리**는 남긴다.

6. **I-3 / I-2** "~라는 것이다 / ~다는 뜻이다 / ~라는 것이 확인된다" 결산 종결 — 문단을 닫을 때 형식명사 '것'으로 명제를 한 번 더 감싸는 습관이 **10회 이상** 반복된다. 한 겹 벗기면 그대로 더 단단한 단정문이 되는데, 그 한 겹이 문장을 설명조로 늘어뜨린다.
   · 근거: `100%라는 숫자가 재고 있는 것이 …아니라는 뜻이다`(1절) / `100% 대기 시간으로만 전환된 것이다`(5-2) / `슬롯이 다 찼다는 것.`(5-2) / `큐에서 기다린 것이다`(5-3) / `실제보다 커지는 것이다`(5-6) / `요청 수라는 것이 관측으로 확인된다`(5-8) / `같은 사건이라는 것이 두 측정으로 교차 확인된다`(5-8) / `오른쪽으로 이동한다는 것이 이 표의 핵심이다`(5-1) / `일정하다는 것이 그 증거다`(5-6) / `선택지가 있다는 뜻이다`(7절)
   → 처방: 한 겹을 벗겨 **동사로 끝낸다**. `같은 사건이라는 것이 두 측정으로 교차 확인된다` → `두 측정이 같은 사건을 가리킨다`. `요청 수라는 것이 관측으로 확인된다` → `요청 수임을 관측이 확인해 준다`. 전부 고치지 말고 **연속으로 붙어 나오는 5-6·5-8 구간을 우선** 정리한다.

## 과윤문(voice flattening) 소견 — 1차 윤문이 남긴 흔적

요청하신 항목. **원저자의 짧고 단정적인 문장이 길고 완만한 설명조로 바뀐 흔적이 실재한다.** 근거 세 가지:

- **템플릿 재사용의 결정적 증거**: `이유는 2절에서 설명한 구조에 있다.`가 5-4와 5-7에 **토씨 하나 다르지 않게 두 번** 등장한다. 사람이 같은 글 안에서 같은 문장을 두 번 쓰는 일은 드물다. 문장 생성기가 같은 자리에 같은 부품을 끼운 자국이다.
   → 처방: 둘 중 하나는 반드시 다르게 쓴다. 5-7 쪽은 `앞 절의 좌석표 비유가 여기서 그대로 되풀이된다` 식으로 그 절의 맥락에 붙인다.
- **완충 문장 삽입**: 위 지배 패턴 3번(C-4/C-6)의 안내문 10여 개가 전형적인 과윤문 산물이다. 원저자라면 표를 던지고 곧장 해석으로 들어갔을 자리에, "이제 ~를 살펴보자"류의 무정보 문장이 한 칸씩 끼어 있다.
- **원문 훼손 의심 지점 1곳**: 5-9의 `5-8의 궤적 같다.`는 한국어로 어색하다. 원래는 `5-8의 궤적 그대로다` 또는 `5-8의 궤적과 같다`였을 가능성이 높다. **의미가 "일치한다"는 강한 주장인데 "같다"로 눌러써서 추측처럼 읽힌다.**
   → 처방: `5-8의 궤적 그대로다.`로 복원 방향 수정. (이것은 윤문이 아니라 **오류 교정**이므로 반드시 반영)

**반대로, 살아 있는 사람 목소리는 이미 충분히 있다 — 이 부분은 절대 건드리지 않는다:**
`> **측정 반복에 대한 정직한 고백**`(4절) / `JSON만 있으면 내가 지어냈을 가능성을 배제할 수 없다`(5-9) / `인과는 정황이다`(한계3) / `자리를 지키고 앉아 있어야 한다`(2절) / `커서가 한참 안 깜빡인다`(5-4) / `쓰레기 숫자가 나온다`(7절) / `source는 성공한 척한다`(부록). 이 문장들이 이 글의 저자성이다.

## 정량 앵커 (수동 집계 — 이번 런은 metrics 블록이 없는 `01_input.txt` 입력)

> 코드 카운트 블록이 입력에 부착되지 않아 정량 앵커는 육안 집계다. 아래 수치는 근사치이며, 확정 증거가 아니라 **밀도 지표**로 읽는다.

- 볼드 스팬 (J-1): 본문·표 합산 **200개 내외**. 표 셀 제외 산문만 **약 90개**. 볼드 없는 산문 문단 극소수
- 산문 내 부연 대시 — (J-3): **약 18회** (헤딩 부제 대시 14개는 별도, 보존 대상)
- "A가 아니라 B" 대구 (C-8): **20회 이상**
- 형식명사 '것' 결산 종결 (I-3/I-2): **10회 이상**
- 열거 도입 "N가지" (D-3): **7회**
- 따옴표 강조 (J-2): **15회 이상** — 기준(5회+) 초과이나, 대부분이 *인용된 오개념*("GPU가 포화했나 보다")이거나 *교재 문구 인용*이라 기능적이다. **지배 패턴으로 지목하지 않는다.**
- "즉" (H-4): **6~7회** — 경계선. 지배 패턴 아님, 5-6·6절에 몰린 것만 관찰 기록
- **깨끗한 항목(건드릴 것 없음)**: A-8 이중피동 **0회** / A-9 "~에 의해" 피동 **0회** / A-16 영어 대명사 직역 **0회** / A-19 이중 조사(-에서의·-으로의) **0회** / A-1 "~에 대해" **0회** / F-1 정도부사 중독 **없음** / G 계열 hedging **없음**(오히려 단정이 강한 글) / C-5 이모지 — 🎯 1회 + 표 안 ✅❌★뿐이라 **남발 아님**

## 보존 지침 (이 글에서 건드리면 안 되는 것)

**절대 불변 — 한 글자도 손대지 않는다:**
- **모든 표** (헤더·구분선·셀 내용·셀 안의 볼드·✅❌★ 마커 전부)
- **모든 코드블록**: bash / promql / 언어 미지정 블록, 그 안의 개행·들여쓰기·주석·`└ c=4 ┘` 아스키 아트까지
- **모든 수치**: 113 / 2,627 / 23배 / 0.0156 / 130.660 / 59.50x / 28.77x / 684배 / +0.3% 등 전부. **단위·자릿수·쉼표 표기도 그대로**
- **메트릭·식별자명**: `vllm:num_requests_running` `vllm:num_requests_waiting` `vllm:time_to_first_token_seconds_bucket` `--max-num-seqs` `max-model-len` `gpu-memory-utilization` `Qwen/Qwen2.5-1.5B-Instruct` `vllm/vllm-openai:v0.23.0` `strategy: Recreate` `startupProbe` 등 백틱 안 전부
- **파일 경로**: `results/*.json` `b1-timeline.txt` `environment.md` `screenshots/proof-0{1,2,3}-*.jpg` 등
- **이미지 링크와 alt 텍스트 전부** — alt는 그림 설명이자 접근성 자산이다
- **`<aside>` 태그와 그 내부**
- **참고 자료 절의 링크·문서명 전부**

**구조 불변:**
- **헤딩 텍스트를 절대 수정하지 않는다.** 문서 상단 목차의 앵커(`#5-1-처리량--슬롯이-천장을-정한다` 등)가 헤딩 텍스트에서 생성되므로, **헤딩을 한 글자만 고쳐도 목차 링크 14개가 전부 깨진다.** 헤딩의 대시(—)·번호·★ 마커도 같은 이유로 보존.
- 목차 블록 자체도 불변
- 헤딩 레벨(`##` `###`), 표·코드펜스·불릿·번호 목록의 구조, 인용 블록(`>`) 위치
- `---` 구분선 위치

**내용 불변:**
- 논증의 인과·조건 관계를 바꾸지 않는다. 특히 5-6의 `잔차 = 고정 오버헤드, 큐 아님` 결론과 한계3의 `인과는 정황이다`라는 **유보의 강도**를 강화하거나 약화하지 않는다
- 없던 주장을 넣지 않는다. 이 글은 "1회 측정"·"nvidia-smi로 확인 안 함"을 스스로 밝힌 글이다. 윤문이 확신을 덧입히면 **글의 정직성이 훼손된다**
- 교재 인용(`CH3` `CH4` `cost-optimized vs latency-optimized` `모범 사례 3번/4번`)의 지시 대상 불변
